type Mutation {
    register(user: UserRegisterationInput!): AuthenticationResponse!
    # TODO: registerFarm(farm: FarmRegisterationInput!): FarmRegisterationResponse!
    unregister(id: UUID!): QueryResponse!
}
